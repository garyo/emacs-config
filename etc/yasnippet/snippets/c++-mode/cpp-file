# -*- mode: snippet -*-
# name: C++ CPP source file header
# key: cpp-file-header
# --
/*`(make-string 60 ?*)`*/
/*`(make-string 60 ? )`*/
/*  Copyright `(format-time-string "%Y")` `(format "%-43s" company-name)`*/
/*  All Rights Reserved.`(make-string 38 ? )`*/
/*`(make-string 60 ? )`*/
/*`(make-string 60 ?*)`*/

#include "`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`.h"

$0
